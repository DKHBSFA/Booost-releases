# Booost — Update Feed

Public auto-update feed for the **Booost** desktop app (delivered via electron-updater).
Each release here carries the platform installers and the `latest*.yml` manifests the app reads to detect updates.

The application source code lives in a separate, private repository.
